<h1>Listing diseases</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Was ist die Krankheit?</th>
    <th>Wie infiziert man sich?</th>
    <th>Symptome</th>
    <th>Vorbeugung und Bekämpfung</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @diseases.each do |disease| %>
    <tr>
      <td><%= disease.name %></td>
      <td><%= disease.what %></td>
      <td><%= disease.infection %></td>
      <td><%= disease.symptom %></td>
      <td><%= disease.counteraction %></td>
      <td><%= link_to 'Show', disease %></td>
      <td><%= link_to 'Edit', edit_disease_path(disease) %></td>
      <td><%= link_to 'Delete', disease, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Disease', new_disease_path %>